{
  "name": "@kiwi/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "REST API over the ledger, the metric engine and the report executor.",
  "scripts": {
    "dev": "node --experimental-transform-types --no-warnings src/main.ts"
  },
  "dependencies": {
    "@kiwi/core": "workspace:*",
    "@kiwi/ledger": "workspace:*",
    "@kiwi/metrics": "workspace:*",
    "@kiwi/model-router": "workspace:*",
    "@kiwi/report-spec": "workspace:*",
    "@kiwi/store": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.2.0"
  }
}
